describe(`Signer`, () => {
  xit('TODO Must get positive receipt');
  xit('TODO Must get negative receipt');
  xit('TODO Must get rejected txn (unable to estimate)');
  xit('TODO Must get rejected txn (capping reached)');
});
